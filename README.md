# h — 离线 HTML → 公开链接

由 `html-open` skill 自动维护的 GitHub Pages 托管仓库。

把任意离线 `.html` 推送到本仓库后，即可通过下面的地址公开访问：

```
https://acelit.github.io/h/<文件名>.html
```

不要手动删除 `.nojekyll`（它让 Pages 原样提供 HTML，跳过 Jekyll 处理）。
